# audio module

Sound configuration — PulseAudio/PipeWire and Bluetooth audio.

## What it configures

- Base audio (PulseAudio or PipeWire)
- Bluetooth audio support

## Key options

- `audio.enableBluetooth` — enable Bluetooth audio (bool)
- `audio.pulseaudio` — enable PulseAudio (bool)
