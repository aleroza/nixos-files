# virtualization module

Container and VM runtime support.

## What it configures

- Docker

## Key options

- `virtualization.docker.enable` — enable Docker daemon (bool)
- `virtualization.docker.enableNvidia` — nvidia-docker support (bool)
