# input module

Keyboard, mouse, and touchpad configuration via libinput.

## What it configures

- Base input settings
- libinput device configuration

## Key options

- `input.libinput.touchpad` — libinput touchpad settings (accelSpeed, tapping, etc.)
- `input.libinput.mouse` — libinput mouse settings
