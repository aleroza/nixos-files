# users module

System user accounts and groups.

## What it configures

- User account definitions
- Group membership

## Key options

- `users.users` — attribute set of users (name, uid, description, extraGroups)
- `users.groups` — attribute set of groups (name, gid)
